<form class ="navbar-form navbar-left" role ="search" action ="/sales/search" method="post">
       <div class = "form-group">
      <input type="text" class="form-control" placeholder="Search" id="search" name="searchVal">
  <input type="submit">
</div>
</form>
<form action="/sales/add" method="POST">

<select name ="product_id">

	{{#products}}
	<option value = "{{product_id}}">{{product_name}}</option>
	{{/products}}
	</select>

	sales_date: <input type="date" name="sales_date" value="{{sales_date}}">
	sales_price: <input type="price" name="sales_price" value="{{sales_price}}">
	qty: <input type="number" name="qty" value="{{qty}}">

	<input type="submit">
</form>

<h1>Sales Records</h1>
<div class="container">
<table class="col-xs-12">

	<tr><thead>
	<th>product name</th>
	<th>sales-date</th>
	<th>sales-price</th>
	<th>qty</th>
	<th>edit/delete</th>

</tr></thead>
<tbody>
 {{#sales}}
<tr>
<td>{{product_name}}</td>
<td>{{sales_date}}</td>
<td>R{{sales_price}}</td>
<td>{{qty}}</td>

<td>
	<a href="/sales/editSales/{{sale_id}}">Edit</a>
     <a href="/sales/delete/{{sale_id}}">Delete</a>
</td>
</tr>
{{/sales}}
</tbody>
</table>
</div>
